# 2026-07-29 开发记录

## 计划

- 将现有牙膏素材替换为无品牌、透明背景的柔和卡通3D模型。
- 保持儿童端界面、图片布局、手势判定和21个步骤逻辑不变。
- 本地验证后推送至 GitHub Pages。

## 完成

- 生成白色软管、蓝色旋盖、红蓝装饰的无品牌3D牙膏模型。
- 完成透明背景处理，输出为带 Alpha 通道的 `toothpaste.png`。
- 将原素材保留为 `toothpaste-original.png`，便于回退。
- 仅更新 `index.html` 中两个牙膏图片缓存版本号，未修改步骤和交互代码。

## 验证

- JavaScript 语法检查通过。
- `LEVELS` 静态核对仍为3项技能、21个步骤。
- 浏览器实际进入刷牙第2步，新牙膏完整加载，尺寸为1536×1024，缓存版本为 `v=5`。
- 首页与 `toothpaste.png` 本地 HTTP 请求均返回 `200 OK`。

## 连续动画设计

- 用户确认采用“动作示范＋前序状态保留”的完整方案。
- 示范在步骤进入约2秒后循环播放，儿童首次触摸时立即暂停。
- 已完成21步动画映射、状态结构、性能约束和测试标准设计。
- 科研保护规则已确认：普通训练和干预教学启用动画；基线评估和维持期默认关闭。
- 当前仅新增设计文档，尚未修改游戏代码。
- 已完成连续动画执行计划，开发顺序为基础控制、状态保留、三项技能动画、科研条件验证和21步回归测试。

## 连续动画 Task 1

- 新增 AnimationPolicy：普通训练启用，研究模式仅在干预期的教学模式启用。
- 新增 AnimationController：步骤进入后延迟2000毫秒启动，首次触摸暂停，切换步骤、开始关卡和返回首页时清理。
- 新增动画暂停与减少动态效果的 CSS 基础约定，未改动21步交互、数据写入、提示等级、页面布局或素材。
- Node 内置测试通过3项：科研条件、控制器延迟/暂停/清理、步骤进入/触摸/返回首页连接点。
- JavaScript 语法与策略检查通过；3项技能各7步静态核对通过。

## 连续动画 Task 2

- 新增三项技能各7步的连续场景状态映射，以及可重置、记录完成状态和查询状态的 SkillSceneState。
- 在原始开始关卡、返回首页和步骤成功路径接入状态重置与更新，未新增研究事件或改动既有21步判定、数据写入、提示等级、布局和素材。
- 场景包装器现接收关卡和步骤参数，并输出仅限前序已完成步骤的非交互状态标记，供后续动画任务使用。
- TDD：先新增状态模型与场景包装测试并确认因 STEP_STATE_KEYS 缺失而失败，最小实现后 Node 内置测试8项全部通过。
- 额外核对：JavaScript 可编译，三项技能状态映射均为7项。

## 连续动画 Task 3

- 刷牙七步均增加不可交互的独立示范标记，示范只会在既有的两秒延迟后以 `.demo-running` 播放，首次触摸仍由既有控制器暂停。
- 第4、5、7步使用独立的牙刷或毛巾示范影子，真实拖拽物保持原位，未改动拖拽阈值、步骤判定、数据写入、素材或提示。
- 新增牙膏残留、左侧清洁、双侧清洁和第7步成功后的嘴部闪光等连续状态装饰；装饰层均不可点击且不参与成功判定。
- TDD：先扩展连续动画测试并确认3项新增测试因标记、影子示范和最终状态缺失而失败；最小实现后11项测试全部通过，刷牙标记静态核对为7/7。
- 桌面浏览器验收受当前安全限制阻断：本地 HTTP 预览会暴露整个工作区而被拒绝，本地文件 URL 也被浏览器策略拦截；未使用替代绕过方式。

## 连续动画 Task 3 Fix Round 1

- 将第7步最终嘴巴闪光从通用 `.interaction-area.success` 改为当前步骤层的专用 `brush-final-clean` 状态，避免视觉和语音提示的临时高亮提前显示完成反馈。
- 专用状态仅在既有真实第7步成功流程完成状态写入后立即添加，不等待下一次场景重绘，也未改动提示系统或数据流程。
- TDD：先确认视觉提示会添加通用高亮且原CSS选择器会命中，再以失败测试锁定“提示不能显示最终闪光、真实完成必须显示”的边界；修复后连续动画测试13项全部通过。
- 新增示范动画时长边界测试，七个刷牙示范均在1.4至2.4秒范围内。

## 连续动画 Task 4

- 洗脸七步均增加不可交互的独立示范标记：上推水龙头、接水、打湿与拧干毛巾、擦脸、冲洗毛巾和关水龙头。
- 新增水流、收水、毛巾搓洗、擦脸和关水的循环动画；示范继续使用既有两秒延迟和首次触摸暂停控制，不参与步骤完成或记录写入。
- 连续状态现依次呈现第1步后的水流、第2步后的手部水滴、第3步后的湿毛巾、第4步后的减少水滴、第5步后的洁净脸部闪光与第6步后的干净湿毛巾；第7步不再保留水流。
- TDD：先新增洗脸示范和状态测试，确认15项中新增2项失败（缺少 `demo-wash-1`，第7步仍显示水流）；最小实现后15项全部通过，洗脸标记静态核对为7/7，抽取的 JavaScript 语法检查通过。
- 本轮本地浏览器视觉检查未能执行：浏览器控制环境没有可用浏览器实例。其余自动检查已完成。

## 连续动画 Task 5

- 为穿衣七步新增独立、不可交互的示范层：找到外套、正面翻转、左右袖口、向下拉衣身、向上拉拉链和左右整理衣领。
- 第3至第7步均使用单独的示范小手，真实小手、袖口、衣身、拉链和衣领目标区域保持原有位置与35%重叠判定不变；示范小手会先抵达可见目标，再执行方向动作。
- 连续状态使用与外套一致的内联SVG覆盖层，依次显示正面高光、左袖、双袖、平整下摆、闭合拉链，并在真实完成第7步时显示对称衣领闪光。
- TDD：先增加3项穿衣示范回归测试并确认因七步标记、动画路径和衣领完成状态缺失而失败；最小实现后连续动画测试19项全部通过。
- 静态核对穿衣标记为7/7，抽取网页JavaScript语法检查通过，`git diff --check`通过；浏览器控制环境没有可用实例，未能完成7步桌面视觉验收。

## 连续动画 Task 5 Fix Round 1

- 将衣领完成闪光从 `.demo-running` 的循环闪光组中移除，示范动画和通用提示成功状态均不会显示该反馈；仅真实完成穿衣第7步后由 `dress-final-collar` 显示。
- 将第6步独立拉链示范小手在到达与向上动作阶段左移20px，使其与未改动的40×90拉链目标重叠55.6%，高于原有35%阈值；真实小手、目标区域和判定阈值均未改动。
- TDD：先新增衣领闪光隔离和拉链尺寸重叠回归测试，确认21项中新增2项失败；最小修复后21项全部通过，并完成抽取JavaScript语法和差异检查。

## 连续动画 Task 6

- 补齐研究条件自动化矩阵：普通训练三种训练模式均启用动画；研究模式下仅干预期教学模式启用，干预期练习和评估、基线期及维持期三种模式均关闭。
- 补齐 Task 1 延后覆盖：桌面端 `mousedown` 首次按下暂停示范，开始新关卡会清除上一关的示范状态后再安排新步骤。
- 自动化验证两次无儿童操作的示范启动不增加 `UnifiedDataManager.events` 或 `researchRecords`；一次真实完成仅增加一个 `step_success` 事件和一条步骤记录，`responseTimeMs` 以精确毫秒记录。
- TDD RED：完整记录字段测试稳定失败，确认 `UnifiedDataManager.onStepComplete()` 的步骤记录缺少既有统一数据结构要求的 `taskId`。
- 在用户明确授权后，仅在既有步骤记录对象中补入 `taskId: level.id`，没有增加新的写入路径或其他字段；GREEN 后 Node 自动化测试25项全部通过。
- 抽取网页脚本语法检查和 `git diff --check` 均通过。浏览器控制环境无可用实例，未进行桌面或 iPad 手工视觉验证。

## 连续动画 Task 6 Fix Round 1

- 将动画无写入验证改为真实 `startLevel → loadStep` 路径：先保留正常产生的 `session_start`、`step_start`、`ltm_chain_start`，再以快照验证示范启动和两个最长 CSS 周期等效时间不增加事件或步骤记录。
- 该测试明确关闭 `promptState.enabled`，隔离与动画无关的 Least-to-Most 提示升级事件，同时保留普通训练条件下的 `AnimationPolicy` 启用状态。
- 将真实完成验证改为示范播放后通过交互区域的 `mousedown → mouseup` 完成点击步骤，确认首次鼠标按下暂停示范、数据仅增加一个 `step_success` 和一条记录，并继续核对字段集合、`taskId` 和毫秒用时。
- 开始关卡测试现记录清理、安装新步骤和安排示范的调用顺序，确认清理发生在安装和安排之前。
- 突变验证已在 Fix Round 2 强化为仅在测试运行时向动画迭代事件监听器注入事件写入，数据差异断言按预期捕获该变化；未发现新的生产缺陷，因此未改动 `index.html`。Node 自动化测试26项全部通过。

## 连续动画 Task 6 Fix Round 2

- VM 场景层现保留示范子元素，并支持同一事件的多监听器、`once` 选项及实际事件分发；被动完整性测试在真实示范元素上分发两次 `animationiteration`，而非仅推进模拟时钟。
- 迭代突变验证仅在测试源副本中注册会写入事件的 `animationiteration` 监听器，数据差异断言按预期失败，证明可捕获动画循环中的意外写入；提交的网页代码未变。
- 新增对原始 `function startLevel` 的源码顺序契约：首个 `AnimationController.clear()` 必须早于 `state.currentLevel` 赋值和 `loadStep(0)`；移除该清理调用的突变会失败，避免被 `loadStep` 内部清理掩盖。
- 未发现新的生产缺陷，未改动 `index.html`；Node 自动化测试27项全部通过。

## 连续动画 Task 7（可自动化部分）

- 依据任务限制，仅执行静态和 Node 自动化核查；浏览器运行环境不可用，因此没有进行桌面浏览器或 iPad Safari 测试，也没有推送、部署或合并。
- JavaScript 语法检查通过；`LEVELS` 静态核对为 3 项技能、21 个步骤；`git diff --check` 通过。
- 完整命令 `node --test tests/continuous-animation.test.js` 通过 27 项、失败 0 项。自动化覆盖研究条件策略、两秒延迟、首次触摸/鼠标按下暂停、返回首页与切换关卡清理、持续状态重置、被动动画无重复数据写入、写入突变检出及真实完成记录契约。
- 新增 `docs/testing/2026-07-29-continuous-animation-checklist.md`，逐行列出三关 21 步的自动化证据和待人工项目，并附可由用户执行的 iPad Safari 点击、滑动、拖拽、剩余步骤、重置及研究条件核查流程。
- 由于桌面视觉、桌面触控、桌面音频、浏览器控制台和 iPad Safari 仍为待人工核查，本任务状态为 `DONE_WITH_CONCERNS`；未改动 `index.html`，不使用原计划的完成/部署提交文案。
