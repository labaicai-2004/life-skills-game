# 2026-09-19 开发记录

本轮仅修复最终复审剩余的最高提示等级记账时机问题，基准为 `c2827b2`。`applyPromptLevel(4)` 原先安排两秒后的示范，同时立即把 `promptUsed` 提升为 4；儿童在等待期间完成，或示范被研究策略、减少动态效果设置禁止时，也会误记为 L4。

先增加四项真实计时器与鼠标完成路径的回归检查：等待 1999ms 后完成、基线策略禁示范、减少动态效果禁示范、2000ms 真正开始示范后完成。修复前四项均因 `4 !== 3` 失败。应用代码仅将 L4 分支末尾 `break` 改为 `return`，跳过提前赋值，继续由 `AnimationController` 真正显示示范时的既有路径记为 4。示范前完成保留最后实际提供的 L3，真实显示示范后完成记为 L4；被动动画不产生成功或子步骤记录。

最终运行 `node --test tests/new-target-behaviors.test.js tests/continuous-animation.test.js`：115 项通过、0 失败、0 跳过，退出码 0；`git diff --check` 通过。未上传，桌面实际渲染、听音和实体 iPad Safari 验收仍待完成。

顺带检查 `replayVoice()`：目前只重播指令，不改变提示等级。现有规范区分语音提示与独立完成，但没有明确定义儿童主动点击“再听一次”是重发任务指令还是教师语音辅助。因此这次不扩大为重播按钮研究编码变更，将其作为待明确的研究协议问题保留；若研究中把所有指令重播都算 L3，应另行统一该路径。

## 本地合并、图片清理复核与上传准备

已将 `codex/new-target-behaviors` 合并回本地 `main`，合并提交为 `aebf37e`。合并后重新运行完整检查：115 项通过、0 项失败，临时开发工作区和已合并分支已安全清理，未改动原有 `.superpowers/brainstorm/` 构思资料。

上传前逐项复核图片清单：当前 Git 跟踪的 PNG/JPG 共 12 张，全部位于 `assets/laundry/`、`assets/folding/` 和 `assets/umbrella/`，均为新三关正在使用的素材。旧刷牙、洗脸、穿衣的 14 张根目录图片已在前次开发提交中删除，本次没有发现可继续删除的未使用图片。测试文件中的旧图片名称是“不得再引用”的负向检查，旧设计文档中的名称作为历史记录保留，两者都不是实际运行素材。本轮不误删新图片，不改写历史文档。

已将 `main` 上传至 GitHub，GitHub Pages 线上首页已更新。线上 `index.html` 与本地文件的 SHA-256 均为 `70b69a5e510ef9b30afca20e7593ff106e75d75feb0e9124ce3c8e58432893ec`，页面包含“清洗衣服、折叠衣服、收整折叠伞”三个新入口；12 张新素材的线上请求全部返回 HTTP 200。此次线上检查确认了版本和资源同步，完整 21 步真实触控、语音听感及实体 iPad Safari 仍保留为人工验收项。

## 研究者模式三名儿童管理计划

用户确认使用同一台设备管理 `001`、`002`、`003` 三名儿童，并保留首页星星 2 秒内连续点击 5 次的隐藏入口。本轮计划在现有 Research Mode 上做最小增强：增加已用儿童编号快捷选择，仍保留手动输入新编号；研究看板可按儿童编号筛选；可分别导出当前儿童或一次导出全部儿童 CSV。数据继续追加到本机 `researchRecords`，不引入服务器、账号或外部依赖，旧记录与原 CSV 字段保持兼容。

实施使用独立工作区 `.worktrees/researcher-mode`；基线为 115 项自动检查全部通过。将先增加儿童选择、看板筛选与导出范围的失败检查，再修改 `index.html`，最后运行完整回归检查和本地页面核对。

已完成三名儿童的同设备管理：研究设置页会自动汇总已用编号并显示 `001`、`002`、`003` 等快捷按钮，仍可手动输入新编号；数据看板增加按儿童筛选，支持导出当前儿童或全部儿童 CSV。同时修复了从研究设置页打开看板时被上层窗口遮挡、普通学习记录按钮重复显示的问题，看板关闭后会恢复原按钮。

本地浏览器实际点击已验证：首页星星 2 秒内连点 5 次可打开隐藏入口；依次输入并启动 `001`、`002`、`003` 后，面板同时显示三个快捷按钮；“查看全部研究数据”可直接显示看板、全部儿童导出按钮与关闭操作。浏览器验收用的三个编号只存在本地测试页数据中，未写入项目文件。

最终运行 `node --test tests/new-target-behaviors.test.js tests/continuous-animation.test.js`：120 项通过、0 失败、0 跳过；`git diff --check` 通过。本轮仍保留在独立开发分支，尚未合并或上传线上页面。

代码复审后补充了数据边界修正：“全部儿童”看板与 CSV 排除无儿童编号的普通训练记录；研究会话会把儿童编号传入会话摘要，再次选择该儿童时能自动建议下一个训练次数；看板对恢复文件中的所有动态字段进行显示转义；原“当前 Session”按钮改为语义准确的“查看当前儿童数据”，未输入编号时不可点击。同时为看板增加竖向滚动，所有数据按钮保证至少 44px 高。

复审修正后重新运行完整检查：123 项通过、0 失败、0 跳过；`git diff --check` 通过。

用户选择将功能合并到本地 `main`，合并后再次运行 123 项完整检查，全部通过。随后用户确认上传，已将研究者模式多儿童管理推送到 GitHub `main`。GitHub Pages 线上 `index.html` 与本地 SHA-256 均为 `5099f3d1e9e7e72e8992aabeb9bc0411c3f03d7f79465df67371aa8f4e8ca7b3`，已确认包含“查看当前儿童数据”、`ResearchParticipants` 和“导出全部儿童”功能。
