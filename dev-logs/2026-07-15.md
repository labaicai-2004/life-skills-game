# 开发日志 — 2026-07-15

## V2.1 升级：Research Mode 入口 + Session 初始化

### 修改内容

#### 1. 隐藏研究入口
- [x] 首页标题🌟改为**连续点击5次**进入研究模式（替换原来的长按）
- [x] 2秒内点够5次触发，超时重置计数
- [x] 入口完全隐藏，不影响儿童使用

#### 2. Session 初始化
- [x] 点击 Start Session 后保存 `researchSession` 到 localStorage
- [x] 数据结构：
```json
{
  "participantID": "P001",
  "skill": "brush",
  "phase": "baseline",
  "sessionNumber": 1,
  "startTime": "2026-07-15T10:30:00.000Z"
}
```

#### 3. 数据字段对齐
- [x] 增加 `participantID` 字段（兼容原有 `childId`）
- [x] 仪表盘和CSV导出优先使用 `participantID`
- [x] Session 摘要同步包含 `participantID`

---

## 保持不变

- [x] 儿童端 21 个步骤交互完全不变
- [x] UI 视觉风格不变
- [x] 三个关卡正常运行
- [x] 单文件架构，零外部依赖
- [x] iPad Safari 兼容

---

## 当前状态

- **版本**：V2.1
- **代码行数**：2493 行
- **线上地址**：https://labaicai-2004.github.io/life-skills-game/

---

## 研究模式使用方式

1. 首页标题🌟连续点击 **5 次**
2. 填写 Participant ID / 选择 Skill / 选择 Phase / 输入 Session#
3. 点击"开始研究训练"
4. 系统自动跳转训练关卡，全程记录数据
5. 完成后自动下载 JSON+CSV 报告

---

## 涉及文件

| 文件 | 修改内容 |
|------|---------|
| `index.html` | 入口改为5-tap + researchSession保存 + participantID字段 |
| `dev-logs/2026-07-15.md` | 本日志 |

---

## Phase 1 (P0.1) 数据架构清理

### 修改内容

#### 1. 删除空注释残留块
- [x] 删除 `BEHAVIOR DATA COLLECTION MODULE` 空注释块（行 1902-1905）
- [x] 删除 `V2.2 BEHAVIOR TRACKING SYSTEM` 空注释块（行 2628-2631）
- [x] UnifiedDataManager 注释更新为简洁单行描述

#### 2. 字段统一
- [x] `participantId` → `participantID`（UnifiedDataManager 内部属性 + session_summaries + SessionManager 别名）
- [x] `responseTime`(秒) → `responseTimeMs`(毫秒)（researchRecords 记录 + CSV 导出 + Dashboard 显示）
- [x] `errors` 保持不变（已经是正确字段名）

#### 3. HOOK 调用直接化
- [x] 所有 `ResearchCollector.xxx` → `UnifiedDataManager.xxx`（共 ~20 处）
- [x] 所有 `SessionManager.xxx` → `UnifiedDataManager.xxx`（共 4 处）
- [x] 所有 `BehaviorTracker.xxx` → `UnifiedDataManager.xxx`（Observer 面板 2 处）
- [x] 向后兼容别名（ResearchCollector / BehaviorTracker / SessionManager）保留但不使用

#### 4. localStorage 写入点
- [x] `_logEvent()` 仅写入 session 内存（this.events），不写 localStorage ✅
- [x] `researchRecords` — 唯一主要数据写入目标 ✅
- [x] `session_summaries` — 保留，字段名已对齐 ✅
- [x] 无 `research_<sessionId>` 写入（Phase 1 已清理）✅

#### 5. Dashboard & CSV 导出适配
- [x] `showDashboard()` 读取 `responseTimeMs`，显示时转为秒
- [x] `exportResearchCSV()` 列头改为 `responseTimeMs`

### 验证结果
- [x] JS 括号匹配：523 开 = 523 闭 ✅
- [x] 所有关键 API 方法存在 ✅
- [x] 所有关键引用存在 ✅
- [x] HTTP 200 页面可加载 ✅
- [x] 零处旧名称引用残留（ResearchCollector./BehaviorTracker./SessionManager. 在活跃代码中）✅
- [x] 零处 `participantId`（小写 d）引用 ✅
- [x] 零处裸 `responseTime`（不含 Ms）引用 ✅

### 不影响
- [x] 儿童端 21 步骤逻辑
- [x] UI 视觉
- [x] 向后兼容别名（保留供外部调用）
