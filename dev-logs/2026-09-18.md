# 2026-09-18 开发记录

## 计划

- [x] 审计旧任务文字、运行时图片引用与图片 SHA-256。
- [x] 更新新三关的项目文档、素材说明和研究记录说明。
- [x] 增加运行时旧图片引用和静态图片路径存在性检查。

## 完成情况

- 新三关运行时只引用 `assets/laundry/`、`assets/folding/`、`assets/umbrella/` 内的本地卡通图片。
- 根目录旧 PNG/JPG 未出现精确哈希重复；其中不被网页或当前规范作为资源加载的旧运行图片已在本任务中移除，测试以负向断言确认不再引用。
- 自动检查覆盖图片尺寸、透明通道、三关实际生成页面与静态页面的图片路径、旧图片引用和三关互动；95 项通过。后续仍需在桌面与 iPad Safari 完整走查 21 步。

## Task 9：本地验证证据与限制

- 验证基准为 `776f8c3`。重新运行 `node --test tests/new-target-behaviors.test.js tests/continuous-animation.test.js`：95 项通过、0 失败、0 跳过，退出码 0；`git diff --check` 通过。本次未修改应用代码。
- 自动检查覆盖三关各 7 步、21 组被动示范标记、真实事件处理函数的模拟鼠标与触摸输入、两面各 3 次揉洗、冲洗连续停留 2 秒、逐片整理 6 片伞布、卷伞等待 1500ms、35% 扣带重叠、连续状态、完成弹窗调用与会话只结束一次。测试使用模拟 DOM/时钟，不等同于浏览器中亲手走通。
- 静态复核确认教学/练习/评估三种模式配置存在；练习从视觉提示开始，评估最大提示等级为 0，研究模式的被动示范仅在干预期教学模式启用。记录包含 `taskId`、`stepNumber`、`promptLevel`、`trainingMode`、`responseTimeMs`、`errors`；有效重复动作写入 `substep_complete` 及子进度/伞片编号，历史记录保留。
- 语音代码为 `zh-CN`、语速 `0.75`，优先选择系统中已安装的中文女声；实际声音由设备可用音色决定，本次未听音验收。静态触控尺寸、页面 viewport 和 touch-action 已核查，但未经浏览器布局实测。
- 启动了仅监听本机的 `http://127.0.0.1:8097/`。读取首页返回 `HTTP/1.0 200 OK`、`Content-type: text/html`、`Content-Length: 172449`。验证后已停止本次启动的 Python 进程 20572。
- 浏览器控制不可用：CUA 的浏览器清单为空，获取原生 Chrome 的调用被中断。因此桌面真实 21 步、控制台错误、横竖屏视觉/实际触控尺寸和女声听感均未实测；本机也没有可用的实体 iPad Safari 验证结果。本轮未上传或发布。

### 待人工验收

1. 在桌面浏览器从首页逐关完成 21 步，确认中文指令、延迟示范、箭头、目标区、成功反馈、下一步和完成弹窗，并查看控制台是否报错。
2. 确认洗衣正面与背面各需 3 次揉洗，冲洗需停留约 2 秒；折衣各折叠状态连续保留；雨伞需逐片整理完 6 片、卷伞动画结束后才通过，最后扣好固定带。
3. 使用虚构参与者编号分别检查教学、练习、评估模式及导出记录；评估模式不出现被动示范，核对任务、步骤、提示等级、用时、重复动作记录。
4. 在实体 iPad Safari 横屏、竖屏各检查一遍，确认触控区域够大、拖放能命中、无意外缩放、画面无遮挡、语音自然且速度合适。

## 最终审查修复

本轮计划逐项验证最终审查提出的六个问题，先补可复现的失败检查，再修正折衣触控和即时完成图、提示与研究记录、无效动作计数、退出计时器及历史摘要保留。已完成全部六项；此前 Task 9 的“示范仅教学启用”和“95 项”是修复前快照，以本节及最终修复报告为准。

- 折衣第 3–6 步改为相对 300px 衣物定位，测试同时读取实际 CSS 最小尺寸与动作配置，验证 750px 和 360px 舞台的可见部位覆盖及起终点分离。真实成功事件会立即更新当前图层，轮廓逐步收窄；第 7 步当场显示叠好卫衣并播放“衣服叠得真整齐”。
- 新指引、高亮和示范统一遵循 L0–L4；教学和练习可在干预期显示示范，评估始终不显示。移除 L4 自动代做，保留真实手势完成；实际示范至少记为 L4。新增 2.1 秒教学/评估、示范后记录与新场景进入时提示同步检查。
- 洗衣和伞的无效真实动作走统一 `step_error` 和 `errors`，操作取消不计。已覆盖鼠标与触摸下“取消、无效、正确完成”的真实事件路径。
- 200ms 提示初始化、350ms 指令、提示升级与场景版本/关卡/步骤绑定；退出、切关和成功会取消旧链。退出动画的过期回调不能重新安装监听。新增快速退出、快速切关、完成后延迟回调、250ms 步骤过渡检查。
- 移除摘要的 50 条裁剪；用 55 条旧摘要与旧研究记录验证完成新会话后全部保留。庆祝彩纸由 120 个降为 50 个。
- 桌面浏览器和 iPad Safari 真机仍须最终验收；本修复轮采用实际运行函数、模拟 DOM/时钟、鼠标/触摸事件和 CSS 几何检查，不能替代浏览器渲染和听音验收。未上传。
- 最终组合检查：`node --test tests/new-target-behaviors.test.js tests/continuous-animation.test.js` 共 111 项通过、0 失败、0 跳过；`git diff --check` 通过。保留原 95 项覆盖并增加 16 项回归；原自动代做和评估示范的期望随修复契约更新。
