# 执行步骤文档

## 当前实施状态

本轮将原有三项训练替换为“清洗衣服、折叠薄厚适中的衣服、收整折叠伞”。三关各 7 步，共 21 步，已在单一 `index.html` 中完成；详细设计与执行计划保存在 `docs/superpowers/`。本文件保留项目的日常维护入口。

| 阶段 | 名称 | 状态 | 本轮结果 |
|---|---|---|---|
| P0 | 核心流程 | ✅ 完成 | 三个新任务 ID、三关各 7 步和关卡完成流程已建立 |
| P1 | 统一卡通视觉 | ✅ 完成 | 三张场景底图、九张透明物品图均在本地 `assets/` 中 |
| P2 | 互动与连续状态 | ✅ 完成 | 35% 拖放、40px 滑动、重复动作子进度和跨步骤状态保留已完成 |
| P3 | 语音与正向反馈 | ✅ 完成 | 中文女声 0.75 倍、慢速示范、箭头提示、星星奖励已接入 |
| P4 | 自动检查与发布准备 | 🔄 进行中 | 自动检查已覆盖，待桌面完整走查与用户 iPad Safari 真机检查 |
| P5 | 后续扩展 | 🔲 待开始 | 家长查看、学习汇总、更多生活技能和难度梯度 |

## 本轮已完成内容

### 关卡与图片

- [x] 第一关“清洗衣服”：入盆、浸湿、洗衣液、正反面各 3 次揉洗、冲洗停留、双手拧干。
- [x] 第二关“折叠衣服”：同一件薄厚适中的长袖卫衣平铺、抚平、两袖、两边衣身和下摆折好，状态连续保留。
- [x] 第三关“收整折叠伞”：收伞、缩杆、扣带朝外、六片伞布依次整理、合拢、自动完成约 1.5 圈卷伞、扣住固定带；不包含伞套或书包。
- [x] 所有运行时图片改用 `assets/laundry/`、`assets/folding/`、`assets/umbrella/` 内的本地卡通 PNG。
- [x] 完成旧图片引用审计；只删除已确认不被运行页面、测试和当前规范使用的根目录旧图片。

### 互动、提示和记录

- [x] 每屏一个主要操作，触控目标不小于 44×44pt，拖放覆盖超过 35% 即通过。
- [x] `StepProgress` 支持揉洗 3 次和六片伞布 6 次；每次有效重复动作产生子步骤记录。
- [x] 慢速示范为不可交互图层，真实触摸会停止示范；持续状态只在真实完成后进入下一步。
- [x] 新研究技能映射为 `clothes washing`、`clothes folding`、`folding umbrella`，历史 `researchRecords` 和会话摘要保持兼容。
- [x] 关卡结束统一使用既有完成流程，避免重复结束会话。

## 检查清单

- [x] `node --test tests/new-target-behaviors.test.js tests/continuous-animation.test.js`：94 项通过。
- [x] 图片存在、尺寸、透明通道、运行时旧图片引用和静态 `src` 路径均有自动检查。
- [ ] 桌面浏览器按首页入口完整走完三关 21 步。
- [ ] 在 iPad Safari 上走完三关，重点确认拖放、重复揉洗、六片伞布、卷伞与扣带触控。
- [ ] 经用户确认后推送到线上地址。

## 日常维护规范

1. 修改前阅读本文件和相关设计规范，并在 `dev-logs/` 记录当天计划。
2. 应用代码只修改 `index.html`，不引入框架、CDN 或第三方依赖；图片放在项目目录内。
3. 每次修改后先运行相关自动检查，再在桌面浏览器检查受影响步骤；涉及行为或视觉规范时同步更新文档。
4. 仅在素材确认没有运行页面、测试和当前规范引用时，使用显式文件名删除；不删除外部参考图或历史资料。
5. 修改稳定且获得用户确认后，提交并推送更新。

## 后续方向

优先收集 iPad 真机中的具体卡点，例如哪一步拖不到、哪一片伞布不清楚、语音是否自然，再逐项修复。后续不在未确认需求时扩展第四关、网络账户或复杂管理页面。

*文档版本：v3.0 | 更新日期：2026-09-18*
